if code is already there in the local and not present in the git
Do the following
- `git init`
- `git add .`
- `git commit -m "hi"`

- Now go to git hub and create a repo without readme


- There will be a command called `git remote blah blah`
- `git push`